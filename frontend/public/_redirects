# Static assets should be served directly, not redirected
/_next/static/* /_next/static/:splat 200
/_next/* /_next/:splat 200
/favicon.ico /favicon.ico 200
/*.css /:splat 200
/*.js /:splat 200
/fix-mime.js /fix-mime.js 200

# API routes (if any) 
/api/* /api/:splat 200

# SPA Fallback for client-side routing (only for non-asset requests)
/* /index.html 200 