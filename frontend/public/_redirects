# Static assets must be served as-is, never redirected
/_next/static/css/*.css 200!
/_next/static/chunks/*.js 200!
/_next/static/*.js 200!
/_next/static/* 200!
/_next/* 200!
/favicon.ico 200!

# API routes (if any)
/api/* 200

# SPA fallback only for HTML pages
/* /index.html 200 